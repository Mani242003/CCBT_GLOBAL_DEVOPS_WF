name: CMO Build Workflow

on:
  workflow_call:
    inputs:
      relno:
        type: string
        required: true

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Print release number
        run: echo "Release is ${{ inputs.relno }}"
